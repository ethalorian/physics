// AI image generation (Google Imagen) was removed at Craig's request — the app
// does not use Imagen. This module is intentionally empty; the figure block and
// lesson hero use upload / paste-URL only. (File kept as a no-op because the
// repo mount can't delete files from here — safe to delete in git.)
export {}
