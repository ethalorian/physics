/**
 * Design-system kit — the prototype's language as reusable primitives.
 *
 * Import from here: `import { SectionLabel, StatPill, ProgressTrack } from '@/components/ds'`
 *
 * These sit on top of the shadcn primitives (Card, Button, Input) and the
 * tokens/type-scale in globals.css. See docs/DESIGN_SYSTEM.md for the full spec
 * and the patterns (solve box, sequence chips, hero card) that aren't yet
 * extracted into components.
 */
export { SectionLabel } from './SectionLabel'
export { StatPill } from './StatPill'
export { ProgressTrack } from './ProgressTrack'
